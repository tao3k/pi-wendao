---
name: simple-file-counter
description: Count TypeScript files in a directory
---

# Simple File Counter

Count all TypeScript files in the current directory.

## Steps

1. Run `find . -name "*.ts"` to list TypeScript files
2. Count the results
3. Report the count
